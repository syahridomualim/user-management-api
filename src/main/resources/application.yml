server:
  port: ${PORT:8080}

spring:
  datasource:
    url: ${DATABASE_USERNAME:jdbc:mysql://185.2.168.15:3306/pelajar1_user_db}
    password: ${DATABASE_PASSWORD:4r6i3;XIUJ+2ji}
    username: ${DATABASE_USERNAME:pelajar1_user}
  jpa:
    show-ql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect